!!!
%html
  %head
    %title Todo
    = stylesheet_link_tag    "application", media: "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
  .row-fluid
    .span10.offset1
      .hero-unit.text-center{:id => 'heroUnit'}
        %h1{:id => 'soloMato'}
          SoloMato
        .text-center
          %strong{:id => 'currentUser'}= 'Welcome, ' + current_user.email
        %p{:id => 'profileText'}
          Stay focused and don't quit!
        .text-center
          = link_to 'New task', new_task_path, class: 'btn btn-success', remote: true
          = link_to 'Logout', destroy_user_session_path, class: 'btn btn-danger', method: :delete
  %div{:id => 'flash'}
    = render "shared/flash"
    = yield
