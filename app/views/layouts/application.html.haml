!!!
%html
  %head
    %title Todo
    = stylesheet_link_tag    "application", media: "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
  .row-fluid
    .span10.offset1
      .hero-unit.text-center
        %h1{:id => 'soloMato'}
          SoloMato
        .text-center
          %strong= current_user.email
        %p{:id => 'splashText'}
          Welcome to your task list. Stay focused and keep don't quit!

        .text-center
          = link_to 'New task', new_task_path, class: 'btn btn-success', remote: true
          = link_to 'logout', destroy_user_session_path, class: 'btn btn-danger', method: :delete
    = yield
